# 轻量化教务系统优化方案

## 1. 文档目标

本文基于当前轻量化教务系统的已有能力，对比成熟学生信息系统（Student Information System, SIS）的常见设计思路，整理适合本项目落地的第一、第二优先级优化方案。

当前系统已经具备认证登录、角色权限、课程管理、多时段选课、课表查询、成绩草稿/发布、管理员用户维护等核心功能。后续优化不建议盲目扩展成大型高校教务平台，而应围绕轻量化目标增强三个方向：

- 让学生少判断：系统主动提示学分、冲突、课程状态。
- 让教师少遗漏：成绩发布前自动检查缺录和异常。
- 让管理员看得见全局：用统计面板快速掌握系统运行状态。

## 2. 优化边界与设计原则

### 2.1 项目定位

本系统定位为小型培训班、课题组、课程设计或小规模教务场景使用，目标并发用户数较低。因此优化应遵循：

- 不引入复杂微服务、工作流引擎或前后端分离架构。
- 继续使用 Spring Boot + Thymeleaf + Vanilla JS + MySQL。
- 尽量复用现有实体、Repository、Service 和前端样式。
- 每个优化点都要能独立演示，避免多个功能强耦合。

### 2.2 成熟信息系统可借鉴点

成熟 SIS 通常会重点建设：

- 选课前校验与学业规划：避免学生提交后才发现冲突。
- 学业进度可视化：帮助学生理解当前学习状态。
- 教师操作前检查：减少成绩发布、课程管理中的遗漏。
- 管理驾驶舱：让管理员快速看到课程、选课、成绩的整体情况。
- 数据驱动提醒：系统主动发现风险，而不是等用户自己查。

这些能力可以被轻量化改造，不需要一次性实现完整的大型教务流程。

## 3. 第一优先级优化

第一优先级建议优先实现三个功能：预选篮、学生首页学业状态面板、管理员运营看板。这三个功能改动范围可控，展示效果明显，并且能直接利用现有数据。

### 3.1 学生预选篮与方案检测

#### 3.1.1 需求背景

当前学生选课通常是看到课程后直接点击选课，系统在提交时校验人数、学分和时间冲突。这种方式能保证业务正确，但学生体验上存在一个问题：学生需要反复试错，提交失败后再重新选择。

预选篮的目标是让学生在正式选课前，先临时组合一组选课方案，系统实时告诉他这套方案是否可行。

#### 3.1.2 用户价值

- 学生可以先规划，再一次性提交。
- 时间冲突、学分超限、课程满员等问题提前暴露。
- 选课页面更接近成熟教务系统体验，演示亮点明显。

#### 3.1.3 功能说明

学生在课程列表中可以执行：

- 加入预选篮：将课程临时加入当前页面的待选方案。
- 移出预选篮：从待选方案中移除课程。
- 检查方案：展示该方案的学分总数、课程数量、冲突情况、容量状态。
- 一键确认选课：对预选篮中尚未正式选中的课程依次提交选课。

预选篮可以先做成前端状态，不必新增数据库表。页面刷新后预选篮清空即可，符合轻量化定位。

#### 3.1.4 校验规则

预选篮中需要提示以下风险：

| 检查项 | 规则 | 提示方式 |
|---|---|---|
| 学分上限 | 已选学分 + 预选篮学分不得超过 30 | 红色风险提示 |
| 时间冲突 | 预选课程之间、预选课程与已选课程之间存在相同 schedule | 标记冲突课程 |
| 容量不足 | enrolledCount >= maxStudents | 禁止确认或给出警告 |
| 重复选择 | 已选课程不能重复加入 | 按钮置灰或提示已选 |

#### 3.1.5 页面设计建议

在 `courses.html` 中增加右侧或底部固定区域：

- 预选课程列表
- 总学分
- 风险提示
- “清空预选篮”按钮
- “确认选课”按钮

对于小屏幕，可以将预选篮显示为页面底部折叠面板。

#### 3.1.6 后端接口建议

最小实现可以不新增接口，直接复用现有选课接口。但为了让“检查方案”更准确，建议新增一个轻量接口：

```http
POST /api/enrollments/preview
```

请求体示例：

```json
{
  "courseIds": [1, 2, 3]
}
```

返回体仍使用 `Result<T>`：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "totalCredits": 9.0,
    "availableCredits": 21.0,
    "valid": false,
    "warnings": [
      "数据库原理与应用 与 操作系统原理与Linux应用 时间冲突：周三 10:20-12:00"
    ]
  }
}
```

#### 3.1.7 建议新增 DTO

- `EnrollmentPreviewRequest`
  - `List<Long> courseIds`

- `EnrollmentPreviewVO`
  - `Double totalCredits`
  - `Double selectedCredits`
  - `Double availableCredits`
  - `Boolean valid`
  - `List<String> warnings`
  - `List<Long> conflictCourseIds`

#### 3.1.8 实现顺序

1. 在后端封装预览校验逻辑，复用现有学分和时间冲突规则。
2. 新增 `/api/enrollments/preview` 接口。
3. 在 `courses.html` 中增加前端预选篮状态。
4. 调用 preview 接口展示风险。
5. 确认选课时逐个调用正式选课接口，并刷新课程列表。

#### 3.1.9 验收标准

- 学生可以把多门未选课程加入预选篮。
- 预选篮能显示总学分。
- 超过 30 学分时明确提示。
- 存在时间冲突时能指出冲突课程。
- 满员课程不能被正常确认选课。
- 确认选课成功后，课程状态和已选人数刷新。

### 3.2 学生首页学业状态面板

#### 3.2.1 需求背景

当前功能入口比较清晰，但学生登录后仍需要分别进入课程、课表、成绩页面才能了解自己的整体状态。成熟信息系统通常会在首页汇总“用户最关心的信息”。

对于学生来说，最关心的是：今天有没有课、选了多少学分、成绩有没有发布、是否还有风险。

#### 3.2.2 用户价值

- 学生登录后即可知道当前学习状态。
- 首页从“导航页”升级为“个人工作台”。
- 不增加复杂业务，却显著提升系统完整度。

#### 3.2.3 面板内容建议

首页可增加以下信息卡片：

| 卡片 | 内容 | 数据来源 |
|---|---|---|
| 已选学分 | 当前已选课程总学分 / 30 | EnrollmentRepository |
| 已选课程 | 当前已选课程数量 | EnrollmentRepository |
| 今日课程 | 今天对应星期的课程时段 | CourseSlot + Enrollment |
| 最新成绩 | 最近发布的 3 条成绩 | Enrollment 中成绩字段 |
| 学业提醒 | 学分接近上限、存在未发布成绩、今天无课等 | Service 聚合 |

#### 3.2.4 后端接口建议

新增学生仪表盘接口：

```http
GET /api/dashboard/student
```

返回示例：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "selectedCredits": 12.5,
    "creditLimit": 30.0,
    "selectedCourseCount": 4,
    "todaySchedules": [
      {
        "courseName": "机器学习",
        "teacherName": "曾增日",
        "schedule": "周四 10:20-12:00",
        "location": "教学楼 A101"
      }
    ],
    "latestGrades": [
      {
        "courseName": "数据库原理与应用",
        "score": 88.0,
        "gpaPoint": 3.7
      }
    ],
    "tips": [
      "你当前已选 12.5 学分，还可选择 17.5 学分"
    ]
  }
}
```

#### 3.2.5 建议新增 DTO

- `StudentDashboardVO`
  - `Double selectedCredits`
  - `Double creditLimit`
  - `Integer selectedCourseCount`
  - `List<ScheduleVO> todaySchedules`
  - `List<GradeVO> latestGrades`
  - `List<String> tips`

#### 3.2.6 页面设计建议

在 `index.html` 中按角色展示不同工作台。学生角色显示：

- 顶部三张统计卡片：已选学分、已选课程、今日课程数。
- 中部今日课程列表。
- 右侧或下方显示最新成绩和系统提醒。

视觉上继续使用现有樱花粉配色，但首页卡片应保持信息密度，不要做成纯装饰页面。

#### 3.2.7 实现顺序

1. 新增 DashboardController 或在现有 Controller 中增加 dashboard 接口。
2. 新增 StudentDashboardVO。
3. 在 Service 中聚合选课、课表、成绩数据。
4. 修改 `index.html`，根据角色调用学生 dashboard 接口。
5. 前端渲染统计卡片、今日课程和最新成绩。

#### 3.2.8 验收标准

- 学生登录首页后能看到已选学分和课程数量。
- 当天有课时能显示课程名、时间、地点。
- 已发布成绩能出现在最新成绩区域。
- 没有数据时页面显示友好的空状态。

### 3.3 管理员运营看板

#### 3.3.1 需求背景

管理员当前可以维护用户，但缺少系统整体运行情况。优秀的信息系统通常会提供管理驾驶舱，让管理员快速了解当前系统是否正常运转。

轻量系统不需要复杂 BI，只需要几个核心统计值和榜单即可。

#### 3.3.2 用户价值

- 管理员快速了解学生、教师、课程、选课状态。
- 能发现热门课程、满员课程、成绩未发布课程。
- 适合作为系统演示的管理端亮点。

#### 3.3.3 看板内容建议

| 模块 | 指标 | 说明 |
|---|---|---|
| 用户概览 | 学生数、教师数、管理员数 | 从 User 表按角色统计 |
| 课程概览 | 课程总数、已分配教师课程数、未分配教师课程数 | 从 Course 表统计 |
| 选课概览 | 总选课人数、平均每门课选课人数 | 从 Enrollment 表统计 |
| 热门课程 | 选课人数 TOP5 | 按 courseId 分组 |
| 满员课程 | enrolledCount >= maxStudents | 提醒管理员 |
| 成绩进度 | 已发布成绩数、草稿成绩数、未录入成绩数 | 从 Enrollment 成绩字段统计 |

#### 3.3.4 后端接口建议

```http
GET /api/admin/dashboard
```

返回示例：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "studentCount": 2,
    "teacherCount": 7,
    "adminCount": 1,
    "courseCount": 7,
    "unassignedCourseCount": 0,
    "enrollmentCount": 16,
    "publishedGradeCount": 8,
    "draftGradeCount": 2,
    "missingGradeCount": 6,
    "popularCourses": [
      {
        "courseName": "机器学习",
        "teacherName": "曾增日",
        "enrolledCount": 5,
        "maxStudents": 30
      }
    ],
    "fullCourses": []
  }
}
```

#### 3.3.5 建议新增 DTO

- `AdminDashboardVO`
  - 用户统计字段
  - 课程统计字段
  - 选课统计字段
  - 成绩统计字段
  - `List<CourseSummaryVO> popularCourses`
  - `List<CourseSummaryVO> fullCourses`

- `CourseSummaryVO`
  - `Long courseId`
  - `String courseName`
  - `String teacherName`
  - `Integer enrolledCount`
  - `Integer maxStudents`

#### 3.3.6 页面设计建议

可以在 `admin/users.html` 上方增加简短看板，也可以新增 `/admin/dashboard` 页面。

更推荐新增 `/admin/dashboard`：

- `/admin/users` 保持用户管理职责清晰。
- `/admin/dashboard` 用于展示统计信息。
- 导航栏给管理员增加“管理看板”入口。

#### 3.3.7 实现顺序

1. 新增 AdminDashboardVO 和 CourseSummaryVO。
2. 在 Repository 中补充统计查询，优先使用 Spring Data 方法或简单 JPQL。
3. 新增 `/api/admin/dashboard`。
4. 新增 `admin/dashboard.html` 页面。
5. 在导航栏按 ADMIN 角色显示入口。

#### 3.3.8 验收标准

- 管理员能看到用户、课程、选课、成绩的关键统计。
- 热门课程按选课人数降序展示。
- 满员课程能被单独列出。
- 普通学生和教师不能访问管理员看板接口。

## 4. 第二优先级优化

第二优先级建议实现轻量培养方案和教师成绩发布检查清单。这两个功能能进一步增强“教务系统”的专业性，但复杂度略高，适合在第一优先级稳定后推进。

### 4.1 轻量培养方案与学业进度

#### 4.1.1 需求背景

成熟教务系统通常包含培养方案、毕业要求、Degree Audit 或 Academic Advisement。完整版本非常复杂，涉及专业、年级、课程类别、替代课程、重修规则等。

本项目可以实现一个轻量版本：只关注学分进度和课程类别，不引入复杂毕业审核。

#### 4.1.2 用户价值

- 学生知道自己已经完成多少学习要求。
- 管理员或教师可以按课程类别组织课程。
- 系统从“选课工具”升级为“学业管理工具”。

#### 4.1.3 轻量规则建议

先定义固定课程类别：

| 类别 | 说明 |
|---|---|
| REQUIRED | 必修 |
| ELECTIVE | 选修 |
| GENERAL | 通识 |
| PE | 体育 |

再定义每类最低学分要求，例如：

| 类别 | 最低要求 |
|---|---|
| 必修 | 12 学分 |
| 选修 | 6 学分 |
| 通识 | 3 学分 |
| 体育 | 1 学分 |
| 总学分 | 22 学分 |

这些数值可以先写入配置或种子数据，不必一开始做复杂后台配置。

#### 4.1.4 数据模型建议

在 `Course` 实体中新增字段：

- `category`
  - 类型可以是枚举 `CourseCategory`
  - 默认值建议为 `ELECTIVE`

可选新增实体：

- `ProgramRequirement`
  - `id`
  - `category`
  - `requiredCredits`
  - `description`

如果想保持更轻量，第一版可以不建 `ProgramRequirement` 表，而是在 Service 中用常量定义要求。

#### 4.1.5 学分计算规则

建议只统计已发布成绩对应的课程学分：

- 已选但未出成绩：不计入已完成学分。
- DRAFT 成绩：不计入已完成学分。
- PUBLISHED 成绩：计入已完成学分。

如果后续需要更精细，可以增加及格线规则：

- `score >= 60` 才计入完成学分。

#### 4.1.6 后端接口建议

```http
GET /api/dashboard/academic-progress
```

返回示例：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "totalRequiredCredits": 22.0,
    "totalCompletedCredits": 9.0,
    "totalProgressPercent": 40.9,
    "items": [
      {
        "category": "REQUIRED",
        "categoryName": "必修",
        "requiredCredits": 12.0,
        "completedCredits": 6.0,
        "progressPercent": 50.0
      }
    ]
  }
}
```

#### 4.1.7 建议新增 DTO

- `AcademicProgressVO`
  - `Double totalRequiredCredits`
  - `Double totalCompletedCredits`
  - `Double totalProgressPercent`
  - `List<AcademicProgressItemVO> items`

- `AcademicProgressItemVO`
  - `String category`
  - `String categoryName`
  - `Double requiredCredits`
  - `Double completedCredits`
  - `Double progressPercent`

#### 4.1.8 页面设计建议

可以先放在学生首页，也可以在成绩页增加“学业进度”区域。

推荐放在学生首页：

- 总进度条：已完成学分 / 要求学分。
- 分类进度条：必修、选修、通识、体育。
- 已完成课程列表可以先不做，避免页面过重。

#### 4.1.9 管理员和教师影响

课程创建和编辑表单需要增加课程类别选择：

- 必修
- 选修
- 通识
- 体育

若不选择，默认选修。

#### 4.1.10 实现顺序

1. 新增 `CourseCategory` 枚举。
2. 在 `Course` 中新增 `category` 字段。
3. 修改课程创建/编辑 DTO 和页面表单。
4. 在 DataInitializer 中给种子课程补充类别。
5. 新增学业进度计算 Service。
6. 在学生首页展示学业进度。

#### 4.1.11 验收标准

- 课程可以设置类别。
- 学生首页能看到总学分完成进度。
- 学生首页能看到各类别完成进度。
- 只有已发布成绩对应的课程会计入完成学分。
- 老数据没有类别时能按默认选修处理。

### 4.2 教师成绩发布检查清单

#### 4.2.1 需求背景

当前成绩流程已经有 DRAFT 和 PUBLISHED 状态，教师可以保存草稿、发布和撤回。这是很好的基础。下一步可以在发布前增加检查清单，避免教师漏录成绩或误发布。

成熟信息系统通常会在关键提交动作前提供数据完整性检查。

#### 4.2.2 用户价值

- 教师发布成绩前能看到是否还有学生未录入。
- 管理员和学生看到的成绩数据更可靠。
- 系统显得更专业，也更符合真实教务流程。

#### 4.2.3 检查内容建议

教师进入某门课程成绩页面时，展示：

| 检查项 | 说明 |
|---|---|
| 选课人数 | 当前课程选课学生总数 |
| 已录入人数 | 有 score 的学生数量 |
| 未录入人数 | 没有 score 的学生数量 |
| 草稿人数 | status = DRAFT 的人数 |
| 已发布人数 | status = PUBLISHED 的人数 |
| 平均分 | 已录入成绩平均值 |
| 最高分 | 已录入成绩最高值 |
| 最低分 | 已录入成绩最低值 |
| 异常成绩 | 小于 0 或大于 100 的成绩，理论上应被后端拦截 |

#### 4.2.4 发布规则建议

建议采用谨慎规则：

- 若存在未录入学生，不允许“一键发布全部成绩”。
- 若全部已录入，可以发布。
- 已发布成绩不可直接修改，继续沿用“先撤回再修改”的现有规则。

如果希望课堂演示更灵活，可以提供“仅发布已录入成绩”按钮，但第一版不建议做，容易增加状态理解成本。

#### 4.2.5 后端接口建议

```http
GET /api/grades/teacher/course/{courseId}/summary
```

返回示例：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "courseId": 1,
    "courseName": "机器学习",
    "enrolledCount": 30,
    "recordedCount": 28,
    "missingCount": 2,
    "draftCount": 28,
    "publishedCount": 0,
    "averageScore": 83.5,
    "maxScore": 98.0,
    "minScore": 61.0,
    "readyToPublish": false,
    "missingStudents": [
      {
        "studentId": 8,
        "studentName": "student"
      }
    ],
    "warnings": [
      "还有 2 名学生未录入成绩，暂不能发布全部成绩"
    ]
  }
}
```

#### 4.2.6 建议新增 DTO

- `GradeSummaryVO`
  - `Long courseId`
  - `String courseName`
  - `Integer enrolledCount`
  - `Integer recordedCount`
  - `Integer missingCount`
  - `Integer draftCount`
  - `Integer publishedCount`
  - `Double averageScore`
  - `Double maxScore`
  - `Double minScore`
  - `Boolean readyToPublish`
  - `List<StudentSimpleVO> missingStudents`
  - `List<String> warnings`

- `StudentSimpleVO`
  - `Long studentId`
  - `String studentName`

#### 4.2.7 页面设计建议

在 `grades.html` 教师视图中，选择课程后在成绩表格上方显示检查清单：

- 左侧统计：选课人数、已录入、未录入、草稿、已发布。
- 右侧成绩分布：平均分、最高分、最低分。
- 下方提醒：未录入学生名单。
- 发布按钮根据 `readyToPublish` 决定是否可用。

不要把检查清单做成很重的图表，保持清晰即可。

#### 4.2.8 实现顺序

1. 在 GradeService 中增加课程成绩统计方法。
2. 新增 `GradeSummaryVO` 和 `StudentSimpleVO`。
3. 新增 summary 接口。
4. 修改发布接口：若存在未录入成绩，则拒绝发布全部。
5. 修改 `grades.html`，在课程成绩列表上方渲染检查清单。

#### 4.2.9 验收标准

- 教师能看到当前课程成绩录入进度。
- 未录入学生能被明确列出。
- 全部成绩录入前，发布按钮不可用或发布接口拒绝。
- 全部录入后可以正常发布。
- 已发布成绩仍遵循先撤回再修改规则。

## 5. 推荐实施路线

建议按以下顺序实现：

1. 学生首页学业状态面板
   - 原因：只做数据聚合，改动风险最低。
   - 验证：学生登录首页即可看到统计信息。

2. 管理员运营看板
   - 原因：同样以统计查询为主，适合作为管理端展示亮点。
   - 验证：管理员能看到全局统计、热门课程和满员课程。

3. 教师成绩发布检查清单
   - 原因：依托现有成绩流程，能提升业务严谨性。
   - 验证：未录入成绩时不能发布全部成绩。

4. 学生预选篮与方案检测
   - 原因：前后端都有改动，体验价值高但实现稍复杂。
   - 验证：预选方案能实时提示冲突、学分和容量问题。

5. 轻量培养方案与学业进度
   - 原因：涉及课程类别字段和历史数据默认值，适合最后做。
   - 验证：已发布成绩能按课程类别汇总为进度条。

## 6. 数据库变更建议

第一优先级功能可以做到不新增核心业务表，只新增 DTO 和统计接口。

第二优先级建议增加：

### 6.1 Course 增加字段

```text
category varchar(32) default 'ELECTIVE'
```

### 6.2 可选新增 ProgramRequirement 表

如果希望培养方案要求可配置，可以新增：

```text
program_requirement
- id
- category
- required_credits
- description
```

第一版可以暂不建表，用 Service 常量实现。

## 7. 风险与取舍

### 7.1 不建议一次性做太复杂

以下功能暂不建议纳入近期实现：

- 完整毕业审核。
- 多专业、多年级培养方案。
- 学费、缴费、财务模块。
- 招生流程。
- 复杂审批流。
- AI 学业顾问。

这些功能会显著增加模型复杂度，和当前轻量化定位不匹配。

### 7.2 优先保证现有流程稳定

新增功能不应破坏现有规则：

- 选课仍由后端最终校验。
- 成绩发布仍以后端状态为准。
- 管理员权限仍通过 `/api/admin/**` 和 `@PreAuthorize` 控制。
- 前端提示只用于提升体验，不能替代后端校验。

## 8. 总结

当前系统已经具备轻量教务系统的核心闭环：登录、课程、选课、课表、成绩、管理。后续最值得升级的不是增加大量新模块，而是把已有数据重新组织成更有帮助的信息：

- 学生端：从“查课程、查成绩”升级为“理解自己的学习状态”。
- 教师端：从“录入成绩”升级为“发布前自动检查完整性”。
- 管理员端：从“维护用户”升级为“掌握系统整体运行情况”。

如果按本文的第一、第二优先级逐步实现，系统会在不牺牲轻量性的前提下，更接近真实优秀信息系统的体验。

